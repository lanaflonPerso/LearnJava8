package com.trl.stream_api;

public class Stream_limit {

    public static void main(String[] args) {

//-----------------------------------------Rabota-s-methodom-limit()--------------------------------------------------

		/*Если опустить упорядочение, то можно также ускорить выполнение метода limit(). Если же требуется обработать
		  любые п элементов из потока данных и при этом неважно, какие из них будут получены, то с этой целью можно
		  сделать следующий вызов:
							Stream<String> sample = words.parallelStream().unordered().limit(n);*/

    }

}
